import numpy as npimport matplotlib.pyplot as pltimport pytesttry:    from patinoire_fct import *except:    pass#---------------------------------------------------------------------------#Définir paramètres du problèmeclass parametre():    zb = 0.05 #[m]    zg = 0.10 #[m]    alpha_b = 0.54 #[ms−1]    alpha_g = 1.203 #[ms−1]    k_b = 0.6 #[Wm−1 K−1]    k_g = 2.1 #[Wm−1 K−1]    h_l = 300 #[Wm−2 K−1]    h_air = 5 #[Wm−2 K−1]    Ti = -1 #[°C]prm = parametre()n=200x = np.linspace(0,90*60,n)y = np.zeros(n)for i in range(n):    y[i] = interpolation_Tair(x[i])temps = [0, 2, 7, 15, 45, 90]  #Valeurs de temps spécifiés dans la questionfor i in range(len(temps)):    temps[i] = temps[i]*60    temperature = [-1, 3.81 , 7.07 , 9.06 , 11.92 , 13.73 ] # plt.plot(temps,temperature, label = 'vrai')# plt.plot(x,y, label = "interpo") # plt.legend()# plt.show   plot = fonction_plot([x,temps],[y , temperature], ["Valeurs d'interpolation","Valeurs spécifiées"],["solid","dashed"], xlabel='Temps (s)', ylabel='Temperature (°C)', title="Valeurs de température d'air interpolées et spécifiées", grid=True, legend=True) 